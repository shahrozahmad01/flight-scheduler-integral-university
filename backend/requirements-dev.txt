# Development requirements for local setup
# Uses SQLite instead of PostgreSQL for easier local development

flask==2.3.3
flask-cors==4.0.0
flask-sqlalchemy==3.1.1
sqlalchemy==2.0.21
python-dotenv==1.0.0
werkzeug==2.3.7
